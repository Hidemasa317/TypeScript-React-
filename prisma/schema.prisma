generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Todo {
  id        String   @id
  text      String
  date      String
  createdAt DateTime @default(now())
}